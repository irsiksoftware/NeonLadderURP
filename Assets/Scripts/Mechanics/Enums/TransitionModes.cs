namespace NeonLadder.Mechanics.Enums
{
    /// <summary>
    /// Modes for scene transition triggering behavior
    /// </summary>
    public enum TransitionMode
    {
        Automatic,      // Trigger on enter
        Interactive     // Press key to trigger
    }
}